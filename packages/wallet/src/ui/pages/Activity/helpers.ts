import type { DayGroup } from '@tezosx/wallet-core/shared/format';

export type { DayGroup };

/** Section order of the activity list. */
export const DAY_ORDER: readonly DayGroup[] = ['Today', 'Yesterday', 'Earlier'];

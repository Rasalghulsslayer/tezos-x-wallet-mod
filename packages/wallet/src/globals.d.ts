/** Build-time version strings injected by Vite `define` (see vite.config.ts),
 *  sourced from the workspace package.json files so the Settings "About" row
 *  never goes stale. */
declare const __WALLET_VERSION__: string;
declare const __CORE_VERSION__: string;
